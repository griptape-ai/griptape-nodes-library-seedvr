[project]
name = "griptape-nodes-library-seedvr"
version = "0.1.0"
description = "Griptape Nodes library for video restoration and upscaling using SeedVR2"
authors = [{ name = "Griptape, Inc.", email = "hello@griptape.ai" }]
readme = "README.md"
requires-python = "~=3.12.0"
dependencies = ["griptape-nodes-engine"]

[tool.uv.sources]
griptape-nodes-engine = { git = "https://github.com/griptape-ai/griptape-nodes-engine", rev = "latest" }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = ["pyright>=1.1.396", "pytest-asyncio>=0.21.0", "ruff>=0.8.0", "pytest"]

[tool.hatch.build.targets.wheel]
packages = ["griptape_nodes_library_seedvr"]

[tool.ruff]
line-length = 120
target-version = "py312"
exclude = [
  ".venv",
  "**/node_modules",
  "**/__pycache__",
  "griptape_nodes_library_seedvr/seedvr",
]

[tool.ruff.lint]
select = [
  "E",  # pycodestyle errors
  "W",  # pycodestyle warnings
  "F",  # Pyflakes
  "I",  # isort
  "B",  # flake8-bugbear
  "C4", # flake8-comprehensions
  "UP", # pyupgrade
]
ignore = [
  "E501", # line too long (handled by formatter)
  "B008", # do not perform function calls in argument defaults
]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "auto"

[tool.pyright]
venvPath = "."
venv = ".venv"
include = ["."]
exclude = [".venv", "**/node_modules", "**/__pycache__", "**/.*", "templates", "griptape_nodes_library_seedvr/seedvr"]
pythonVersion = "3.12"
reportIncompatibleMethodOverride = false
reportMissingImports = false
reportMissingModuleSource = false
